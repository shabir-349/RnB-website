CLAUDE CODE INSTRUCTIONS — Run these as TWO separate prompts in order.

========================================
PROMPT 1 — Backend (paste this into Claude Code):
========================================

Read the file CRTGE-SYSTEM-PROMPT.txt in the repo root. In api/generate-topics.js: (1) Replace the ENTIRE system prompt content with the exact contents of CRTGE-SYSTEM-PROMPT.txt — do not modify, cut, or summarize a single character. (2) Set model to "gpt-4.1-mini". (3) Set temperature to 0.4. (4) Set top_p to 0.9. (5) Add response_format: {"type": "json_object"} to the API call. (6) Set max_tokens to 5000. (7) Change the user message format to send a JSON object: {"population": population, "intervention_or_exposure": intervention || null, "study_design": studyDesign}. (8) Update the response parsing: the new prompt returns {"meta":{...},"candidates":[...]} — extract candidates from response.candidates. Handle both formats: if response has .candidates use that, if response is an array use it directly. Strip any markdown backticks before JSON.parse. (9) After parsing, return the full response object (including meta and candidates) to the frontend, not just the candidates array. Don't break rate limiting, CORS, or auth.

GIT: git add -A && git commit -m "feat: replace TopicScout with CRTGE production prompt and settings" && git push

========================================
PROMPT 2 — Frontend (paste this into Claude Code):
========================================

In the TopicScout frontend (script.js + dashboard.html + style.css), completely rebuild the topic card rendering to match the new CRTGE output schema. The API now returns {"meta":{...},"candidates":[...]}. Extract candidates from response.candidates. Each candidate card should display: (1) Title at top bold gold with composite_score shown as a prominent "85/100" badge on the right. (2) Study Design badge and MA Classification badge side by side — only show ma_classification if not "not_applicable", and show ma_classification_rationale as a tooltip or expandable text below it. (3) PICO section as a compact block: Population, Intervention/Exposure, Comparator (if not null), Outcomes (comma-separated), Timeframe (if not null), Setting (if not null). Style PICO labels in amber and values in light text. (4) Rationale paragraph. (5) Evidence Gap paragraph. (6) Novelty Assessment paragraph. (7) Feasibility Assessment paragraph. (8) Three probability badges in a row: "Prior Synthesis: high", "Sufficient Studies: moderate", "Heterogeneity: low" — color code them: very_low/low = green tones, moderate = amber, high/very_high = red/orange tones. (9) Likely Databases as small tags/pills. (10) Likely Study Types as small tags/pills. (11) Estimated Publication Value as a colored badge (low=gray, moderate=blue, high=amber, very_high=green). (12) Possible Limitations as a compact list. (13) Red Flags section in red/warning style — only show if array is not empty. (14) Make cards collapsible: by default show only title + composite score + study design badge + publication value badge. Click to expand and see all fields. (15) Handle the meta object: show "X candidates generated" at the top. If meta.under_target is true, show a warning banner with the under_target_reason. (16) Update the study design dropdown values to match: LTE, Narrative review, Cross-sectional study, Cohort study, Case control study, RCT, Systematic review and meta-analysis, Network meta-analysis — but send the API values as: letter_to_editor, narrative_review, cross_sectional, cohort, case_control, rct, systematic_review_meta_analysis, network_meta_analysis. Style everything in the existing dark navy + gold theme. Don't break rate limiting or any existing functionality.

GIT: git add -A && git commit -m "feat: rebuild TopicScout UI for CRTGE schema with collapsible cards" && git push
